"""Metrics contract and build subsystem for accounting.

This package contains the canonical metrics implementation. The historical
``accounting.metrics_*`` and ``accounting.build_metric_values`` modules remain
as compatibility wrappers during the package migration.
"""
